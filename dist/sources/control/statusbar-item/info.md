状态栏内容项。用于承载文字、图标或其他轻量控件。

### 参数

#### grow

`boolean` | `string`

是否占用状态栏剩余空间，默认 `false`。

#### type

`'default'` | `'primary'` | `'info'` | `'warning'` | `'danger'` | `'cg'`

语义颜色，默认 `default`。

### 示例

```xml
<statusbar-item type="info">Synchronizing</statusbar-item>
```
