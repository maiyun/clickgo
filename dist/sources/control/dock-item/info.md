Dock 内容项。首次激活时才挂载 slot 内容，并可在切换后保留组件状态。

### 参数

#### name

`string`

必填，当前项的稳定标识。

#### label

`string`

显示名称。

#### icon

`string`

收起模式图标。

#### lazy

`boolean` | `string`

是否首次激活时才挂载，默认 `true`。

#### cache

`boolean` | `string`

激活后是否保留内容实例，默认 `true`。
