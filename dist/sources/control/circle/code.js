import * as clickgo from 'clickgo';
export default class extends clickgo.control.AbstractControl {
    props = {
        'type': 'default',
        'plain': false,
        'sub': false,
        'size': 's',
    };
}
